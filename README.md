# Yield-Prediction-System-repo
A ML system that predicts crop yield (in kg/hectare) for any district in India, given inputs like state, crop type, season, year, rainfall, field area, and fertilizer usage. The system goes from raw CSV files to a live web dashboard where someone can fill a form and get a prediction with a confidence range and historical trend chart.
